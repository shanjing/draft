# Python 3.11 or 3.12 recommended. Python 3.14+ is not supported by chromadb (Pydantic v1).
# NumPy must be <2 for PyTorch/sentence-transformers/Chroma binary compatibility.
numpy>=1.24,<2

# Utilities
tqdm>=4.66.0
einops>=0.7.0
pytest>=7.0.0
python-multipart>=0.0.22
python-dotenv>=1.0.0
click>=8.0
fastapi>=0.100
uvicorn[standard]>=0.22

# RAG
whoosh>=2.7
chromadb>=0.4.22,<0.6
sentence-transformers>=4.37.0
transformers>=4.51.0
tokenizers>=0.13.2
onnxruntime>=1.18.0
onnx>=1.14.0  # required by torch.onnx.export; dev/export only, not needed at inference
# For ONNX-only deployment, remove torch/sentence-transformers/transformers

# MCP server
mcp>=1.0

# AI
anthropic>=0.18.0
openai>=1.0.0
google-generativeai>=0.8.0
google-genai>=1.0

# OpenTelemetry SDK for observability (metrics + traces)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
