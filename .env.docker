# Docker-only overrides (used with: docker run --env-file .env --env-file .env.docker)
# OLLAMA_HOST lets the container reach Ollama on the host (Docker Desktop: host.docker.internal)
OLLAMA_HOST=http://host.docker.internal:11434
