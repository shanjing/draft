[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
markers =
    integration: run against live server (e.g. tests/test_integration_curl.py)
    slow: slow test (e.g. builds RAG index)
filterwarnings =
    ignore::DeprecationWarning
